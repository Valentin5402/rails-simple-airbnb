<!DOCTYPE html>
<html>
  <head>
    <title>RailsSimpleAirbnb</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v6.1.2/css/all.css">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="navbar navbar-light bg-light p-3">
      <%= link_to "Simple Airbnb", flats_path, class: "fw-bold text-decoration-none text-dark" %>

      <%#
      Formulaire pour créer un objet, une instance, etc. : simple_form
      Pour un formulaire "get" (accès à une page) : form_with %>

      <%# <form action="/flats" class="d-flex">
        <input type="text" name="search" class="form-control" placeholder="Search">
        <input type="submit" value="search" class="btn btn-outline-success">
      <%# </form> %>

      <%= form_with url: flats_path, class: "d-flex", method: :get do |form| %>
        <%= form.text_field :search, class: "form-control", placeholder: "Search" %>
        <%= form.submit "Search", class: "btn btn-outline-success" %>
        <%= link_to "Add Flat", new_flat_path, class: "btn btn-primary-success w-50" %>
      <% end %>

    </div>
    <div class="container">
      <%= yield %>
    </div>
  </body>
</html>
